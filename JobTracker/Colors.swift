import SwiftUI

struct AppColors {
    // Table colors
    static let table = Color(white: 0.98)
    static let row = Color.white
    static let shadow = Color.black.opacity(0.1)
    
    // Progress colors
    static let progress = Color.blue
    static let progressBg = Color(white: 0.9)
    
    // Button colors
    static let add = Color.blue
    static let import_ = Color.blue
    static let delete = Color.red
}
